## 5개월차 개념 정리

### 1일차 솔루션 구성 순서

<details>
  <summary>솔루션 구성 순서</summary>

> 최종 솔루션 구성

```cpp
- Client
    - Bin
    - Code
    - Header
    - Include
- Engine
    - Bin
    - Code
    - Header
    - Include
- Reference
    - Header
    - Library
- Base
```

> 생성 과정

1. 빈 솔루션 생성
2. 솔루션 → 추가 → 새 솔루션 폴더 → 00. Client | 01. Engine | 02. Base 각각 생성
3. 솔루션의 저장 폴더에 Base | Reference 폴더 각각 생성 → Reference 폴더에서 Header, Library 폴더 생성
4. 00. Client 폴더에서 추가 → 새 프로젝트 → Window 데스크탑 마법사 → 프로젝트 이름은 Client로 → 데스크톱 애플리케이션과 미리 컴파일된 헤더 설정
5. Client 프로젝트를 솔루션에서 제거 → 솔루션의 저장 폴더에서 Client 폴더로 → Client 폴더에 Bin | Code | Header | Include 폴더를 각각 생성
6. Client 폴더에서 기존에 있던 파일들은 Include 폴더로 이동
7. 00. Client 솔루션 폴더에서 추가 → 기존 프로젝트 → Client/Include/.vcxproj 선택
8. Client 프로젝트 경로 설정 (모든 구성으로 설정)
    1. 일반 → 출력 디렉터리 → ../Bin\
    2. VC++ 디렉터리 →
        1. 포함 디렉터리 → $(DXSDK_DIR)Include
        2. 라이브러리 디렉터리 → $(DXSDK_DIR)Lib/x64
    3. C/C++ → 일반 → 추가 포함 디렉터리 → 
        1. ../Header;
        2. ../../Base
        3. ../../Reference/Header
    4. 링커 → 일반 → 추가 라이브러리 디렉터리 → ../../Reference/Library
    5. 링커 → 입력 → 추가 종속성 → 
        1. d3d9.lib
        2. d3dx9.lib
        3. dxguid.lib
        4. dinput8.lib
        5. Engine.lib
9. 01. Engine 폴더에서 추가 → 새 프로젝트 → Window 데스크탑 마법사 → 프로젝트 이름은 Engine으로 → 동적 연결 라이브러리와 빈 프로젝트 설정
10. Engine 프로젝트를 솔루션에서 제거 → 솔루션의 저장 폴더에서 Engine 폴더로 → Engine 폴더에 Bin | Code | Header | Include 폴더를 각각 생성
11. Engine 폴더에서 기존에 있던 파일들은 Include 폴더로 이동
12. 01. Engine 솔루션 폴더에서 추가 → 기존 프로젝트 → Engine/Include/.vcxproj 선택
13. Engine 프로젝트 경로 설정 (모든 구성으로 설정)
    1. 일반 → 출력 디렉터리 → ../Bin\
    2. VC++ 디렉터리 →
        1. 포함 디렉터리 → $(DXSDK_DIR)Include
        2. 라이브러리 디렉터리 → $(DXSDK_DIR)Lib/x64
    3. 코드가 하나도 없으면 C/C++ 탭이 생기지 않으므로 아무 클래스나 만들기
    4. C/C++ 일반 → 추가 포함 디렉터리 →
        1. ../Header;
        2. ../../Base
    5. 방금 생성한 클래스 삭제
    6. 링커 → 입력 → 추가 종속성 → 
        1. d3d9.lib
        2. d3dx9.lib
        3. dxguid.lib
        4. dinput8.lib
14. 테스트를 위해 ‘4일차-Device 장치 초기화’ 수업 코드에서 다음 코드들을 복붙한다.
    1. Base
    2. Client/Code
    3. Client/Header
    4. Client/Include
        1. pch.h
        2. Client.cpp
    5. Engine/Code
    6. Engine/Header
15. 복붙한 파일들을 각 프로젝트에서 추가 → 기존 항목에서 선택한다.
16. Engine 프로젝트를 먼저 빌드하여 Engine/Bin에 Engine.dll, Engine.lib 파일이 추출되는 것을 확인한다.
17. Engine에서 변경 사항이 있을 경우 항상 해야하는 행동
    1. Reference/Header에 Engine의 바뀐 헤더 파일을 복사
    2. Reference/Library에 Engine.lib를 복사
    3. Client/Bin에 Engine.dll을 복사
18. 이제 Client 프로젝트를 빌드하고, 문제가 없다면 실행할 수 있다.
</details>

---

### 2일차 디바이스 초기화 및 스왑체인 기법

<details>
  <summary>디바이스 초기화 헤더</summary>

> CGraphicDev.h

```cpp
#pragma once

#include "CBase.h"
#include "Engine_Define.h"

BEGIN(Engine)
class ENGINE_DLL CGraphicDev : public CBase
{
	DECLARE_SINGLETON(CGraphicDev)

private:
	explicit CGraphicDev();
	virtual ~CGraphicDev();

public:
  // 디바이스 그리기 명령을 수행할 디바이스 객체 반환.
  // 메인 게임에서 생성 후 반환
	LPDIRECT3DDEVICE9		Get_GraphicDev() { return m_pGraphicDev; }

public:
	HRESULT		Ready_GraphicDev(HWND hWnd, 
								WINMODE eMode, 
								const _uint& iSizeX, 
								const _uint& iSizeY, 
								CGraphicDev** ppGraphicDev);

	void		Render_Begin(D3DXCOLOR Color);
	void		Render_End(HWND hWnd = nullptr);

private:
	LPDIRECT3D9			  m_pSDK;			// 그래픽 카드의 성능을 조사하는 객체
	LPDIRECT3DDEVICE9	m_pGraphicDev;	// 그래픽 카드를 이용하여 그리기 명령을 수행하는 객체(포인터)

private:
	virtual void Free();
};
END
```
</details>

<details>
  <summary>Ready_GraphicDev()</summary>

- 1.Direct3D SDK 생성 : Direct3D를 사용하기 위한 SDK 객체를 생성한다.(그래픽카드 조사 + Device 생성)
```cpp
  m_pSDK = Direct3DCreate9(D3D_SDK_VERSION);
```
- 2.그래픽 카드 성능 조사 : 그래픽 카드의 정보를 저장할 구조체(각종 기능 지원여부 조사)
```cpp
  D3DCAPS9 DeviceCaps;
  ZeroMemory(&DeviceCaps, sizeof(D3DCAPS9));

  if (FAILED(m_pSDK->GetDeviceCaps(D3DADAPTER_DEFAULT, D3DDEVTYPE_HAL, &DeviceCaps)))
	  return E_FAIL;
```
- 3.Vertex Processing 방식 결정
```cpp
  // Device 생성 옵션 저장 변수
	_ulong	dwFlag(0);

  // GPU가 Transform & Lighting을 지원한다면 -> GPU가 담당한다 + 멀티스레드환경
	if (DeviceCaps.DevCaps & D3DDEVCAPS_HWTRANSFORMANDLIGHT)
		dwFlag |= D3DCREATE_HARDWARE_VERTEXPROCESSING | D3DCREATE_MULTITHREADED;
  // GPU가 지원 안한다면 -> CPU가 담당한다 + 멀티스레드환경
	else
		dwFlag |= D3DCREATE_SOFTWARE_VERTEXPROCESSING | D3DCREATE_MULTITHREADED;
```
- 4. Device 생성 정보 설정
```cpp
D3DPRESENT_PARAMETERS		d3dpp;
ZeroMemory(&d3dpp, sizeof(D3DPRESENT_PARAMETERS));

// 후면 버퍼 크기
d3dpp.BackBufferWidth = iSizeX;
d3dpp.BackBufferHeight = iSizeY;

// 후면 버퍼 포맷: 32bit ARGB포맷 사용
d3dpp.BackBufferFormat = D3DFMT_A8R8G8B8;

// 후면 버퍼 개수 : 1개 생성
d3dpp.BackBufferCount = 1;

// 멀티샘플링 : 안티앨러싱을 사용하지 않는다
d3dpp.MultiSampleType = D3DMULTISAMPLE_NONE;
d3dpp.MultiSampleQuality = 0;

// D3DSWAPEFFECT_DISCARD = 스왑 체인 방식,
// D3DSWAPEFFECT_FLIP = 버퍼를 하나두고 번갈아가며 실행,
// D3DSWAPEFFECT_COPY = 더블 버퍼링과 유사한 방식,

d3dpp.SwapEffect = D3DSWAPEFFECT_DISCARD;

// 랜더링항 창 지정: Device가 출력할 윈도우를 지정한다
d3dpp.hDeviceWindow = hWnd;

// 창모드 or 전체화면 : enum WINMODE { MODE_FULL, MODE_WIN };
d3dpp.Windowed = eMode; // 매개변수로 설정

// 깊이 버퍼 생성 : Depth Buffer를 자동으로 설정한다.
d3dpp.EnableAutoDepthStencil = TRUE;

// 깊이 버퍼 포맷 (Depth: 24bit, Stencil: 8bit)
d3dpp.AutoDepthStencilFormat = D3DFMT_D24S8;

// 전체 화면 모드일 때 반영할 화면 주사율
d3dpp.FullScreen_RefreshRateInHz = D3DPRESENT_RATE_DEFAULT;

// VSync 즉시 출력
d3dpp.PresentationInterval = D3DPRESENT_INTERVAL_IMMEDIATE;
```
- 5.Device 생성 및 Device클래스 반환
```cpp
if (FAILED(m_pSDK->CreateDevice(D3DADAPTER_DEFAULT, D3DDEVTYPE_HAL, hWnd, dwFlag, &d3dpp, &m_pGraphicDev)))
{
	MSG_BOX("GraphicDev Create Failed");
	return E_FAIL;
}

// Device 클래스 반환(이후 Get_GraphicDev()를 통해 실제 그림을 그릴 LPDIRECT3DDEVICE9	m_pGraphicDev; 객체를 반환하여 사용)
*ppGraphicDev = this;
```
</details>


<details>
  <summary>Render_Begin()</summary>

- 후면 버퍼를 초기화하고 랜더링을 시작하는 함수
```cpp
// 이전 프레임 화면을 모두 지운다.(color,depth,stencil 버퍼 초기화)
void	CGraphicDev::Render_Begin(D3DXCOLOR Color)
{
	m_pGraphicDev->Clear(0,		// 렉트의 개수
						NULL,	// 렉트의 주소
						D3DCLEAR_STENCIL | D3DCLEAR_TARGET | D3DCLEAR_ZBUFFER,
						Color,		// 후면 버퍼 텍스처 색상(후면버퍼의 색상을 매개변수로 받아서 넣는다.)
						1.f,		// "z버퍼 초기화 값" (NDC후 z값은 0 ~ 1의 값을 가지는데 전부 1로 초기화 하여 가장 먼 거리로 초기화 하는것)
						0);			// 스텐실 버퍼 초기화 값(스텐실: 숫자를 저장하고 예를 들어 stencil == 1인 곳만 그리게 할 수 있음. 일종의 표시 마스크)
  //현재 프레임의 렌더링을 시작한다.
	m_pGraphicDev->BeginScene();
}
```
</details>

<details>
  <summary>Render_End()</summary>

```cpp
// 이번 프레임의 모든 Draw 명령이 끝났음을 알려주는 함수
m_pGraphicDev->EndScene();

// 1. 렉트의 주소
// 2. 렉트의 주소
// 3. 창 핸들(기억해야 함) : null 인 경우 장치 초기화 시, 대입한 핸들로 자동 지정
// 4. 몰라

// 1, 2, 4 매개 변수를 사용하려면 SwapEffect 값이 D3DSWAPEFFECT_COPY 일 때만 사용 가능
// 완성된 후면 버퍼를 화면에 출력한다.
// Front Buffer와 Back Buffer의 역할이 교체된다.
// 포인터에서 주소가 가리키는 값이 서로 교체되는것과 비슷 swap(Front, Back);
m_pGraphicDev->Present(NULL, NULL, hWnd, NULL);
```
</details>

<details>
  <summary>스왑 체인 기법</summary>

> 스왑 체인 기법

Swap Chain은 **화면이 깜빡이는(Flickering) 현상을 방지하기 위해 사용하는 렌더링 방식**이다.

하나의 버퍼에 바로 화면을 그리면 사용자가 그리는 과정을 그대로 보게 되므로 화면이 깜빡이거나 깨져 보일 수 있다.

이를 해결하기 위해 **Front Buffer(전면 버퍼)** 와 **Back Buffer(후면 버퍼)** 를 사용한다.

* **Front Buffer** : 현재 사용자가 보고 있는 화면
* **Back Buffer** : GPU가 다음 프레임을 그리는 작업 공간

---

> 동작 과정

- 1.Back Buffer 초기화

새로운 프레임을 그리기 위해 Back Buffer를 초기화한다.

```cpp
Clear();
```

Color Buffer, Depth Buffer, Stencil Buffer를 모두 초기 상태로 만든다.


- 2.Back Buffer에 렌더링

GPU는 사용자가 보지 않는 Back Buffer에 모든 객체를 그린다.

```text
배경
 ↓
지형
 ↓
플레이어
 ↓
몬스터
 ↓
UI
```

이 과정에서 사용자는 아직 아무것도 볼 수 없다.


- 3.렌더링 종료

```cpp
EndScene();
```

이번 프레임의 모든 Draw 명령이 끝났음을 GPU에 알린다.

이 시점에서도 아직 화면은 변경되지 않는다.

---

- 4.Present()

```cpp
Present();
```

완성된 Back Buffer를 화면에 출력한다.

이때 **버퍼의 내용을 복사하는 것이 아니라 Front Buffer와 Back Buffer의 역할을 교체(Swap)** 한다.

---

> 버퍼 역할 변화

초기 상태

```text
Front(사용자 화면) → Buffer A 

Back(GPU가 렌더링) → Buffer B 
```

`Present()` 호출 후

```text
Front(사용자 화면)  → Buffer B 

Back(GPU가 렌더링)  → Buffer A 
```

다음 프레임에서는 다시 Buffer A를 `Clear()`한 후 새로운 화면을 그리고, `Present()`를 호출하면 역할이 다시 교체된다.

즉, **두 버퍼는 모두 동일한 화면 버퍼이며, 매 프레임마다 Front와 Back의 역할만 번갈아 수행한다.**


> 사용하는 이유

만약 Front Buffer에 직접 렌더링하면 사용자는 그리는 과정을 그대로 보게 된다.

```text
배경
 ↓
플레이어
 ↓
몬스터
 ↓
UI
```

이 과정이 화면에 그대로 나타나면서 **Flickering(깜빡임)** 이 발생한다.

Back Buffer에서 모든 렌더링을 완료한 후 한 번에 화면을 교체하면 사용자는 완성된 프레임만 보게 되므로 부드러운 화면을 출력할 수 있다.

---

> 핵심 정리

* Front Buffer는 현재 화면에 출력되는 버퍼이다.
* Back Buffer는 다음 프레임을 렌더링하는 버퍼이다.
* GPU는 Back Buffer에 모든 객체를 그린다.
* `EndScene()`은 렌더링 명령의 종료를 알린다.
* `Present()`는 Front Buffer와 Back Buffer의 **역할을 교체(Swap)** 하여 완성된 화면을 출력한다.
* 두 버퍼는 매 프레임마다 Front와 Back 역할을 번갈아 수행하며 재사용된다.
* 이를 통해 Flickering 없이 부드러운 화면을 출력할 수 있다.

</details>


---

### 3일차 TimerMgr, FrameMgr, Managment 클래스

<details>
	<summary>TimerMgr / FrameMgr</summary>

> Timer

- Update간격을 측정하는 m_fTimeDelta를 저장하는 용도로 사용함
- 컴퓨터 성능에 따라 프로그램 Update 간격이 다를 수 있고 이를 해결하기 위해 이동 거리나 회전각도에 m_fTimeDelat를 곱해주기 위해 사용

> TimerMgr

- 여러 타이머들을 map컨테이너에 저장하여 관리함
- Get_TimeDelta(const _tchar* pTimerTag) 	: 타이머클래스를 가져옴
- Set_TimeDelta(const _tchar* pTimerTag) 	: 타이머클래스의 타이머 간격(m_fTimeDelat)을 측정함
- Ready_Timer(const _tchar* pTimerTag) 		: 타이머클래스를를 생성하고 map컨테이너에 추가함

> Frame

- IsPermit_Call() 함수를 사용하는 것이 주요 목적.  
- m_fAccTimeDelta	: Timer의 m_fTimeDelta값만큼 누적 시킬 값.
- m_fCallLimit		: m_fAccTimeDelta값의 최대값.

- Ready_Frame(const _float& fCallLimit) 	: m_fCallLimit을 초기화.
- IsPermit_Call(const _float& fTimeDelta) 	: m_fAccTimeDelta를 Timer의 m_fTimeDelta값만큼 증가시키고 m_fCallLimit을 넘어가면 return true;

> FrameMgr

- Fame클래스를 생성하고 관리하는 역할
- Ready_Frame(const _tchar* pFrameTag, const _float& fCallLimit)	: 프레임을 생성하고 map컨테이너에 추가함
- IsPermit_Call(const _tchar* pFrameTag, const _float& fTimeDelta)	: 프레임의 IsPermit_Call()함수를 호출하고 프레임의 IsPermit_Call()함수의 반환값을 그대로 반환함

> 사용 예시

```cpp
게임 루프 시작
        │
        ▼
Timer_Immediate TimeDelta 측정
        │
        ▼
Frame에 TimeDelta 누적
        │
        ▼
60FPS 시간이 되었는가?
        │
 ┌──────┴──────┐
 │             │
아니오        예
 │             │
 │             ▼
 │      fTimer_FPS60 측정
 │             │
 │             ▼
 │      Update(fTimer_FPS60)
 │             │
 │             ▼
 │       LateUpdate()
 │             │
 │             ▼
 │          Render()
 │
 └──────────────→ 다음 루프
```

```cpp
// 타이머 설치 
if (FAILED(CTimerMgr::GetInstance()->Ready_Timer(L"Timer_Immediate")))
    return E_FAIL;

if (FAILED(CTimerMgr::GetInstance()->Ready_Timer(L"Timer_FPS60")))
    return E_FAIL;

// 프레임 설치
if (FAILED(CFrameMgr::GetInstance()->Ready_Frame(L"Frame60", 60.f)))
    return E_FAIL;

// 기본 메시지 루프입니다
while (true)
{
    if (PeekMessage(&msg, nullptr, 0, 0, PM_REMOVE))
    {
        if (msg.message == WM_QUIT)
            break;

        if (!TranslateAccelerator(msg.hwnd, hAccelTable, &msg))
        {
            TranslateMessage(&msg);
            DispatchMessage(&msg);
        }
    }
    else
    {
        CTimerMgr::GetInstance()->Set_TimeDelta(L"Timer_Immediate");
        _float fTimer_Immediate = CTimerMgr::GetInstance()->Get_TimeDelta(L"Timer_Immediate");

        if (CFrameMgr::GetInstance()->IsPermit_Call(L"Frame60", fTimer_Immediate))
        {
            CTimerMgr::GetInstance()->Set_TimeDelta(L"Timer_FPS60");
            _float fTimer_FPS60 = CTimerMgr::GetInstance()->Get_TimeDelta(L"Timer_FPS60");

            pMainApp->Update_MaintApp(fTimer_FPS60);
            pMainApp->LateUpdate_MainApp(fTimer_FPS60);
            pMainApp->Render_MainApp();
        }
    }      
}
```
</details>









