# Windows_api

### 1일차 (260514) 윈도우 초기화

<details>
  <summary>전역 변수</summary>

> 1. HINSTANCE hInst

- 프로그램 자신의 인스턴스(실행 정보)를 저장하는 핸들

> 2. WCHAR szTitle[MAX_LOADSTRING]

-  창 제목 문자열을 저장하는 배열.

> 3. WCHAR szWindowClass[MAX_LOADSTRING]

- 어떤 방식으로 동작하는 창인가? -> 게임 창 or 버튼 창 or 에디트 박스 창
- 창 생성시 1. 창 타입 등록 2. 등록된 타입으로 창 생성
- 창 설계도 종류 같은 느낌

</details>


<details>
  <summary>wWinMain</summary>

- c++의 main()함수의 역할
- windows GUI 프로그램의 시작점

> 선언부
```cpp
int APIENTRY wWinMain(_In_ HINSTANCE hInstance,          // 현재 실핸중인 프로그램의 인스턴스 핸들
                     _In_opt_ HINSTANCE hPrevInstance,   // 이 프로그램이 시작되기 전에 이전 프로그램의 인스턴스를 받는 곳(없으면 NULL)
                     _In_ LPWSTR    lpCmdLine,           // LP(포인터) W(유니코드) STR(문자열)
                     _In_ int       nCmdShow)            // 창 생성 옵션(SW_SHOW, SW_MINIMIZE, SW_MAXIMIZE)

```

> 몸체
```cpp
// 창 제목, 클래스 이름 문자열을 가져옴
LoadStringW(hInstance, IDS_APP_TITLE, szTitle, MAX_LOADSTRING);
LoadStringW(hInstance, IDC_DEFAULTWINDW, szWindowClass, MAX_LOADSTRING);

// 창 클래스 등록 MyRegisterClass() 전역 함수 호출
MyRegisterClass(hInstance);

// 실제 창 생성 InitInstance() 전역 함수 호출 -> 내부에서 CreateWindow 통해 창 생성
if (!InitInstance (hInstance, nCmdShow))
{
    return FALSE;
}

HACCEL hAccelTable = LoadAccelerators(hInstance, MAKEINTRESOURCE(IDC_DEFAULTWINDW));

MSG msg;

// 메시지 루프 진입해서 여기서 프로그램이 작동하는 중
while (GetMessage(&msg, nullptr, 0, 0))
{
    if (!TranslateAccelerator(msg.hwnd, hAccelTable, &msg))
    {
        TranslateMessage(&msg);
        DispatchMessage(&msg);
    }
}

return (int) msg.wParam;


```
</details>


<details>
  <summary>함수</summary>

> MyRegisterClass
```cpp
ATOM MyRegisterClass(HINSTANCE hInstance)
```
- 창 스타일 지정 옵션
- 창 클래스를 등록하는 함수
- `WNDCLASSEXW wcex;` 구조체를 채워넣음
- 마지막에 `RegisterClassExW(&wcex);` 로 등록

> InitInstance
```cpp
BOOL InitInstance(HINSTANCE hInstance, int nCmdShow)
```
- 실제로 창을 만드는 함수
- `CreateWindowW(...)`를 통해 창 생성
```cpp
HWND hWnd = CreateWindow( //HWND 창 핸들을의미
    szWindowClass,
    szTitle,
    WS_OVERLAPPEDWINDOW, // 만들고자하는 윈도우 창의 형태(스타일) 지정 옵션, 현재 값은 기본 창의 형태 옵션을 의미
    CW_USEDEFAULT, 0,    // LEFT,TOP 좌표
    800, 600,            // 가로, 세로 사이즈
    nullptr,             // 만약 부모 윈도우가 있다면 부모 윈도우의 핸들을 지정, 없을 경우 nullptr
    nullptr,             // 윈도우에서 사용할 메뉴의 핸들을 지정
    hInstance,           // 
    nullptr);            // 운영체제가 특수한 목적으로 사용
```
</details>

<details>
    <summary>메시지 루프</summary>

- windows 프로그램은 **이벤트 기반**으로 동작
- 사용자가 키 입력('A' 입력) -> windows가 메시지 생성(WM_KEYDOWN) -> 메시지 큐에 넣음

> GetMessage

- 메시지 큐에서 하나 꺼냄`GetMessage(&msg,nullptr,0,0)`
- 꺼낸 결과를 `MSG msg에 저장`
```cpp
WM_KEYDOWN
WM_MOUSEMOVE
WM_PAINT
```

> MSG 구조체
```cpp
struct MSG
{
    HWND hwnd;
    UNIT message;
    WPARAM wParam;
    LPARAM lParam;
};
```
- 시작시 `msg.message = WM_NULL;` 넣어서 쓰레기값이 들어가는 것을 방지
- 만약 'A'키 입력시
```cpp
message = WM_KEYDOWN
wParam = 'A'
```
> TranslateMessage

- 키보드 입력 메시지를 문자 메시지로 변환
```cpp
TranslateMessage(&msg);
```
- WM_KEYDEOWN -> WM_CHAR로 변환
- 보통 항상 넣음

> DispatchMessage


```cpp
DispatchMessage(&msg);
```
- **메시지를 실제 처리 함수(WndProc)로 보내라**를 수행함
- 내부적으로 아래와 같은 느낌으로 WndProc를 호출
```
WndProc(
    msg.hwnd,
    msg.message,
    msg.wParam,
    msg.lParam
);
```
</details>


<details>
    <summary>WndProc</summary>

```cpp
LREULT CALLBACK WndProc(
    HWND hWnd,            // 메시지를 받을 창의 핸들, 어떤 창에 대한 메시지인가?
    UINT message,         // 어떤 종류의 이벤트인지 나타냄 
    WPARAM wParam,        // 메시지 추가 정보
    LPARAM lParam)        // 
    
```
</details>

---

### 2일차 (260514) 총알 발사

<details>
    <summary>1. 메인 함수 변경점 </summary>

> while문 조건 수정

- GetMessage는 while문의 조건에 적합하지 않아서 true로 변경
- GetMessage(&msg, nullptr, 0, 0) -> PeekMessage(&msg, nullptr, 0, 0, PM_REMOVE) 
- PeekMessage를 통해 종료 메세지가 발생할 때 whlile문을 벗어남

- PeekMessage : 메세지 큐에 메세지가 없는 경우 거짓을 반환
- PM_REMOVE   : 메세지 큐에 메세지가 있을 경우, 가지고 오면서 큐로부터 메세지를 삭제
- PM_NOREMOVE : 메세지 큐에 메세지 유무 상태만 파악, 만약 큐에 있는 메세지를 가져오려면 개별적으로 GetMessage 함수를 다시 호출해야 함

> while문 몸체

- while(true)로 조건 수정하고 while문에서 if(PeekMessage(*msg, nullptr, 0, 0, PM_REMOVE)) else 사용
```cpp
DWORD   dwTime = GetTickCount();    
if(PeekMessage(*msg, nullptr, 0, 0, PM_REMOVE))
{
    // 키보드 입력이 있는 경우 실행할 코드 -> 입력 처리
    if(msg.message == WM_QUIT)
        break;
    if(!TranslateAccelerator(msg.hwnd, hAccelTable, &msg))
    {
        TranslateMessage(&msg);
        DispatchMessage(&msg);
    }
}
else
{
    // 키보드 입력이 없는 경우 실행할 코드 
    if (dwTime + 10 < GetTickCount())              // 너무 빠르게 화면이 렌더링되면 안되니까 GetTickCount()로 조정
    {    
        MainGame.Update();                         // 계산이나 이동
        MainGame.Render();                         // 그리기
        dwTime = GetTickCount();
    }
}
```

</details>


<details>
    <summary>2. InitInstance()함수 변경점</summary>

```cpp
// 그림을 그릴 때는 DC가 필요하고 MainGame에서 GetDC를할 때 매개변수로 사용할 핸들을 전역으로 설정
HWND     g_hWnd;                                                  
BOOL InitInstance(HINSTANCE hInstance, int nCmdShow)
{
    RECT rcWindow{ 0,0, WINCX,WINCY };                            // 게임 창에 사용할 크기를 객체로 만듬
    AdjustWindowRect(&rcWindow, WS_OVERLAPPEDWINDOW, FALSE);      // 제목 표시줄의 크기를 계산하여 rcWindow객체의 값을 수정

    HWND hWnd = CreateWindowW(szWindowClass, 
                             szTitle, 
                             WS_OVERLAPPEDWINDOW,
                             CW_USEDEFAULT, 0, 
                             rcWindow.right - rcWindow.left ,     // 수정된 값을 계산하여 800을 매개변수로 전달
                             rcWindow.bottom - rcWindow.top,      // 수정된 값을 계산하여 600을 매개변수로 전달
                             nullptr, 
                             nullptr, 
                             hInstance, 
                             nullptr);
 	//전역으로 설정한 g_hWnd 전역 변수에 바로 위에서 CreateWindow로 생성한 핸들 포인터를 가르키게 함
    g_hWnd = hWnd;                                                
}
```
</details>

<details>
    <summary>3. CMainGame 추가</summary>

> 중요 변경점

- 기존 TextRPG와 달리 화면이 생겨서 Initialize(),Update(),Release() 함수 외에 Render() 함수가 추가됨
- Render() 함수는 객체를 그리는 함수이고 각 객체의 Render(HDC hDC) 함수를 호출하는 부분
- 객체를 그릴 때는 DC가 필요하기 때문에 DC핸들을 매개변수로 받아야 함.
- 이때 이 매개변수는 전역변수인 g_hWnd핸들로부터 GetDC를 통해 뽑아옴

> Initialize()

- 기존 TextRPG와 동일하게 멤버 변수(포인터)에 동적할당된 주소를 넣어주거나 Set함수를 사용해서 다를 객체를 각 클래스에 넣어줌(Bullet)
- 동적할당을 완료했다면 기존과 동일하게 각 객체의 Initialize()함수를 호출
```cpp
void CMainGame::Initialize()
{
	if (!m_pPlayer)
	{
		m_pPlayer = new CPlayer;
		m_pPlayer->Initialize();
	}
	dynamic_cast<CPlayer*> (m_pPlayer)->SetBullet(&m_BulletList);
	if (!m_pMonster)
	{
		m_pMonster = new CMonster;
		m_pMonster->Initialize();
	}
}
```

> Update()

- 각 객체멤버의 Update() 함수를 호출하거나 매 프레임마다 작동해야 할 코드를 작성.
- Bullet리스트의 경우 반복문을 사용해 각 요소의 Update를 호출.
```cpp
void CMainGame::Update()
{
	m_pPlayer->Update();
	m_pMonster->Update();
	for (auto iter = m_BulletList.begin(); iter != m_BulletList.end();)
	{
		(*iter)->Update();
		if (dynamic_cast<CBullet*> (*iter)->CheckCollison())
		{
			SafeDelete(*iter);
			iter = m_BulletList.erase(iter);
		}
		else
		{
			++iter;
		}
	}
}
```

> Render()

- 각 객체를 그리는 함수를 모아놓는 곳
- 객체가 이동할 때 이전 위치의 객체를 지우는 방식이 아닌 이전 위치의 객체는 배경으로 덮어서 지워지는 것처럼 보이게 함
- 따라서 가장 큰 배경을 제일 먼저 그리고 객체를 그리는 방식을 사용
- 객체의 Render(HDC hDC) 함수를 호출하고 이때 hDC는 CMainGame이 GetDC(g_HWnd)를 통해 뽑아온 m_hDC를 넣어주게 됨
- Bullet리스트의 경우 반복문을 사용해 각 요소의 Render를 호출
```cpp
void CMainGame::Render()
{
	Rectangle(m_hDC, 0, 0, WINCX, WINCY);

	Rectangle(m_hDC, 50, 50, WINCX-50, WINCY-50);

	m_pPlayer->Render(m_hDC);
	m_pMonster->Render(m_hDC);
	for (auto& pBullet : m_BulletList)
		pBullet->Render(m_hDC);
}
```

> Release()

- TextRPG와 동일하게 동적할당한 메모리를 반환하는 함수를 모아놓는 함수
```cpp
void CMainGame::Release()
{
	ReleaseDC(g_hWnd, m_hDC);
	SafeDelete(m_pPlayer);
	SafeDelete(m_pMonster);
	for (auto ptr : m_BulletList)
		SafeDelete(ptr);
	m_BulletList.clear();
}
```
</details>


3. CMainGame




4. CObj




5. CPlayer





6. CBullet







