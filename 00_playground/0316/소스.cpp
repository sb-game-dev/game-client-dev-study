// 메모리 ? > 캐시 > 램 > 하드



#include<iostream> 
//# -> 전처리기 - 컴파일 - 어셈블러 - 링크 - exe파일
// 전처리기: # 뒤에 쓰는 것들(헤더파일), 다른 사람이 이미 만들어놓은 것들이라 문법검사 X
// 헤더파일-> 함수들이 모여있는 것들
// stream -> 흐름
// 
// 
// 컴파일: 문법 검사 + 메모리 빌려오기
// 메모리는 윈도우가 빌려줌
// 윈도우가 메모리 돌려막기함
// 여러 프로그램 돌려야해서
using namespace std;

int main()
{
	cout << "hello world"<<endl;

	return 0;
}