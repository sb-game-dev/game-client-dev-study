#include <iostream>

int main()
{
    // 생성자를 사용하면서 동적 할당을 할 수 있다.
    // 소멸자는 하나의 형태만 존재함 (메모리를 반환하기 전에)
    // 
    // 함수 내부에서 동적할당하면 heap? stack?
    // 
    // c언어의 함수 malloc, free는 생성과 소멸이 안된다.
    // Initialize()는 생성자에 넣을 수 없음 애초에 멤버함수가 만들어지기 전이기 때문
    // 
    // 
}
